import styled from 'styled-components';

export const StChat = styled.body`
    display: flex;
    width: auto;
    margin: 0;
    justify-content: space-between;
    align-items: space-between;
    color: black;
    font-family: 'Play', sans-serif;
    border-radius: 0px;
    position: relative;
    overflow:  hidden;
    touch-action: none;
    height: 100%;
    overflow-x: hidden;
    overflow-y: no-scroll;
`;
